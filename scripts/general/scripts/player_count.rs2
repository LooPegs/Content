[proc,scale_by_playercount](int $base)(int)
//shouldnt use this too much? makes some things harder to balance?

//~22%-100% respawn time. 0-500 players
def_int $playercount = min(playercount, 500);
return (scale(sub(1024, $playercount), 1024, $base));
