[logout,_]()(boolean)
~macro_event_logout;
~set_sleep_logout; //saves sleep timer
return (true);

[if_button,logout:com_8]
if_close;
if (p_finduid(uid) = true) {
    // TODO: re add logout checks
    p_logout;
}

